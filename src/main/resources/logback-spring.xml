<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOG_DIR" value="logs"/>

    <appender name="REQUESTS" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/requests.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/requests.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="com.finance.users.presentation.filter.RequestLoggingFilter" level="INFO" additivity="false">
        <appender-ref ref="REQUESTS"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="com.finance.users.infrastructure.config.DatabaseConnectionLogger" level="INFO" additivity="false">
        <appender-ref ref="REQUESTS"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="com.zaxxer.hikari" level="INFO"/>
    <logger name="com.zaxxer.hikari.HikariConfig" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="REQUESTS"/>
    </root>

</configuration>
